backend = "glx";
blur-background = true;
blur-method = "kawase";
blur-strength = 12;

opacity-rule = [
      "70:class_g = 'Spotify'",
      "80:name *= 'qutebrowser'",
      # "70:class_g = 'Typora'",
];

inactive-dim = 0.3;
mark-ovredir-focused = true;

unredir-if-possible = false;
