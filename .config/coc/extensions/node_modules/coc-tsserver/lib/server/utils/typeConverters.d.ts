/**
 * Helpers for converting FROM LanguageServer types language-server ts types
 */
import * as language from 'vscode-languageserver-protocol';
import Proto from '../protocol';
import { ITypeScriptServiceClient } from '../typescriptService';
export declare namespace Range {
    const fromTextSpan: (span: Proto.TextSpan) => language.Range;
    const toFormattingRequestArgs: (file: string, range: language.Range) => Proto.FormatRequestArgs;
    const toFileRangeRequestArgs: (file: string, range: language.Range) => Proto.FileRangeRequestArgs;
}
export declare namespace Position {
    const fromLocation: (tslocation: Proto.Location) => language.Position;
    const toLocation: (position: language.Position) => Proto.Location;
    const toFileLocationRequestArgs: (file: string, position: language.Position) => Proto.FileLocationRequestArgs;
}
export declare namespace Location {
    const fromTextSpan: (uri: string, tsTextSpan: Proto.TextSpan) => language.Location;
}
export declare namespace TextEdit {
    const fromCodeEdit: (edit: Proto.CodeEdit) => language.TextEdit;
}
export declare namespace WorkspaceEdit {
    function fromFileCodeEdits(client: ITypeScriptServiceClient, edits: Iterable<Proto.FileCodeEdits>): language.WorkspaceEdit;
}
