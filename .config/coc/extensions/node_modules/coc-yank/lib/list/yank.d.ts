import { BasicList, ListContext, ListItem } from 'coc.nvim';
import { Neovim } from '@chemzqm/neovim';
import DB from '../db';
export default class YankList extends BasicList {
    private db;
    readonly name = "yank";
    readonly description = "list of yank history";
    defaultAction: string;
    constructor(nvim: Neovim, db: DB);
    loadItems(_context: ListContext): Promise<ListItem[]>;
}
