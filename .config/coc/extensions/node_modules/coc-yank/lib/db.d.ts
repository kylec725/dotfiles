export interface HistoryItem {
    id: string;
    content: string[];
    regtype: string;
    path: string;
    filetype: string;
}
export default class DB {
    private maxsize;
    private file;
    constructor(directory: string, maxsize: number);
    load(): Promise<HistoryItem[]>;
    add(content: string[], regtype: string, path: string, filetype: string): Promise<void>;
    delete(uid: string): Promise<void>;
}
